import { Inputs, LocalSettings, PluginConfig, ProjectSettings } from "./types";
import { GraphTokenProvider, LogProvider, TelemetryReporter, AzureAccountProvider, AppStudioTokenProvider, SharepointTokenProvider, TreeProvider, PermissionRequestProvider } from "./utils";
import { UserInteraction } from "./qm";
import { CryptoProvider } from "./utils";
import { EnvConfig } from "./schemas/envConfig";
import { ExpServiceProvider } from "./utils/exp";
export interface Context {
    root: string;
    logProvider?: LogProvider;
    telemetryReporter?: TelemetryReporter;
    azureAccountProvider?: AzureAccountProvider;
    graphTokenProvider?: GraphTokenProvider;
    appStudioToken?: AppStudioTokenProvider;
    sharepointTokenProvider?: SharepointTokenProvider;
    treeProvider?: TreeProvider;
    answers?: Inputs;
    projectSettings?: ProjectSettings;
    localSettings?: LocalSettings;
    ui?: UserInteraction;
    cryptoProvider: CryptoProvider;
    permissionRequestProvider?: PermissionRequestProvider;
    expServiceProvider?: ExpServiceProvider;
}
export interface EnvInfo {
    envName: string;
    config: EnvConfig;
    state: Map<string, any>;
}
export interface SolutionContext extends Context {
    envInfo: EnvInfo;
}
export interface PluginContext extends Context {
    envInfo: EnvInfo;
    config: PluginConfig;
}
//# sourceMappingURL=context.d.ts.map